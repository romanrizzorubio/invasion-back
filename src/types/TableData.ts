import {PlayerData} from "./PlayerData";

export type TableData = {
  players: PlayerData[];
  spiderWoman: boolean;
  superLife: number;
  superPlan: number;
  ship: number;
  enemy: number;
  exposed: number;
};
