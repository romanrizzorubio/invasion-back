import {PlayerData} from "./PlayerData";

export type TableData = {
  players: PlayerData[];
  expert: boolean;
  spiderWoman: boolean;
  superDamage: number;
  superThreat: number;
  ship: number;
  enemy: number;
  exposed: number;
};
